{
  "skill_name": "fini-api-agent-configuration",
  "evals": [
    {
      "id": 1,
      "prompt": "Run these five questions against our Fini bot, adjust one guideline to fix the failures, and retest them.",
      "expected_output": "A baseline-test, classify, minimal prompt diff, approval, full-array update, re-fetch, and same-case retest workflow.",
      "assertions": [
        "Fetches the complete current prompt before editing",
        "Classifies failures before deciding the prompt is responsible",
        "Preserves all three prompt arrays in the write",
        "Requires approval for the prompt update",
        "Re-fetches the saved version and reruns the same questions"
      ]
    },
    {
      "id": 2,
      "prompt": "Create an escalation rule from these instructions, but do not make it live yet.",
      "expected_output": "Uses a rule draft, inspects the resulting flow, and leaves bot assignment and publishing for later approval.",
      "assertions": [
        "Generates and inspects a preview, then persists it as an intent DRAFT",
        "Does not assume draft botIds imply live assignment",
        "Reports the persisted rule ID and draft version ID",
        "Reports the review and publish steps still required"
      ]
    },
    {
      "id": 3,
      "prompt": "Import these Zendesk articles into our knowledge base.",
      "expected_output": "Routes to sources and knowledge workflows rather than treating ingestion as agent prompt configuration.",
      "assertions": [
        "Does not use prompt, rule, or tag updates for a source-ingestion request",
        "Routes to fini-api-sources and fini-api-knowledge"
      ]
    },
    {
      "id": 4,
      "prompt": "Refine this escalation rule, then publish the version I just approved to one bot.",
      "expected_output": "Refines the same intent rule as a draft version, avoids an orphan duplicate, and publishes the exact reviewed draftVersionId after confirmation.",
      "assertions": [
        "Reads the full rule and field context before changing the flow",
        "Updates the same intent rule as DRAFT instead of duplicating it",
        "Identifies the exact reviewed draftVersionId",
        "Requires explicit target botIds and publish approval"
      ]
    }
  ]
}
